<%= @install_dir %>/log/*log
{
	rotate 60
	daily
	missingok
	compress
	prerotate
		su -c 'cd <%= @install_dir %>/bin; /usr/bin/python warden_sender_uchoudp.py 1>><%= @install_dir %>/log/warden_sender_uchoudp.log 2>&1' <%= @service_user %>
		rm -f <%= @install_dir %>/log/uchoudp.log.offset
	endscript
	postrotate
		invoke-rc.d uchoudp restart > /dev/null
	endscript
}

