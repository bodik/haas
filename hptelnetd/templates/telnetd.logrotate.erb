<%= @install_dir %>/log/*log
{
	rotate 60
	daily
	missingok
	compress
	sharedscripts
	prerotate
		su -c 'cd <%= @install_dir %>/bin; /usr/bin/python warden_sender_telnetd.py 1>><%= @install_dir %>/log/warden_sender_telnetd.log 2>&1' <%= @service_user %>
		rm -f <%= @install_dir %>/log/telnetd.log.offset
	endscript
	postrotate
		invoke-rc.d telnetd restart > /dev/null
	endscript
}

